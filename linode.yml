---

# Include your site.yml or just use the ceph-ansible sample:
- include: "{{ lookup('env','CEPH_ANSIBLE') }}/site.yml.sample"
  vars:
    public_network: 192.168.128.0/17
    # no cluster network needed, it defaults to public_network
